# TODO: This is a structural placeholder only — not yet used to run the
# project. For now, run the frontend and backend separately (see README).
# Fill in real service definitions once both apps have working Dockerfiles.

version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./datasets:/app/datasets
      - ./outputs:/app/outputs
    # env_file: ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
