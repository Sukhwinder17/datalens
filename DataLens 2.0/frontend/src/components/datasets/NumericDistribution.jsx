import { useState } from "react";
import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export default function NumericDistribution({columns}){const numeric=columns.filter(c=>c.numeric_stats?.histogram?.length);const [selected,setSelected]=useState(numeric[0]?.name||"");const c=numeric.find(x=>x.name===selected);if(!numeric.length)return null;return <div className="rounded-2xl border border-neutral-200 bg-white p-5 shadow-sm dark:border-neutral-800 dark:bg-neutral-900"><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-semibold">Numeric distribution</h3><p className="mt-1 text-xs text-neutral-500">A quick view of how values are spread across the selected numeric column.</p></div><select value={selected} onChange={e=>setSelected(e.target.value)} className="rounded-lg border bg-white px-3 py-2 text-sm dark:border-neutral-700 dark:bg-neutral-950">{numeric.map(x=><option key={x.name}>{x.name}</option>)}</select></div><div className="mt-4 h-64 w-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={c.numeric_stats.histogram}><XAxis dataKey="label" tick={{fontSize:10}} interval="preserveStartEnd"/><YAxis allowDecimals={false} tick={{fontSize:10}}/><Tooltip/><Bar dataKey="count" name="Rows" fill="currentColor"/></BarChart></ResponsiveContainer></div></div>}
