import { AlertTriangle, CheckCircle2 } from "lucide-react";
const labels={high_missing_columns:["High missing values","Some columns are missing at least 50% of their rows."],duplicate_rows_present:["Duplicate rows","The dataset contains repeated complete rows."],constant_columns:["Constant columns","These columns contain only one distinct non-empty value."],high_cardinality_columns:["High-cardinality columns","These columns are nearly unique and may behave like IDs."],mixed_type_columns:["Mixed data types","Some columns contain incompatible value types."],empty_columns:["Empty columns","These columns contain no usable values."],outlier_columns:["Possible outliers","IQR-based screening found unusually distant numeric values."],invalid_date_columns:["Invalid dates","Some values in detected date columns could not be parsed."]};
export default function DataQualityFlags({flags}){const items=[];for(const [key,[title,desc]] of Object.entries(labels)){const value=flags[key];if((Array.isArray(value)&&value.length)||value===true)items.push({title,desc,values:Array.isArray(value)?value:[]});}if(!items.length)return <div className="flex items-center gap-3 rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800"><CheckCircle2 className="h-5 w-5"/><div><b>Looks clean.</b><div className="text-xs opacity-80">No rule-based quality warnings were found.</div></div></div>;return <div className="grid gap-3 md:grid-cols-2">{items.map(i=><div key={i.title} className="rounded-2xl border border-amber-200 bg-amber-50/70 p-4"><div className="flex gap-3"><AlertTriangle className="mt-0.5 h-5 w-5 shrink-0 text-amber-600"/><div><div className="font-semibold text-amber-900">{i.title}</div><div className="mt-1 text-xs text-amber-800">{i.desc}</div>{i.values.length>0&&<div className="mt-2 flex flex-wrap gap-1">{i.values.map(v=><span key={v} className="rounded-md bg-white/70 px-2 py-1 text-xs text-amber-900">{v}</span>)}</div>}</div></div></div>)}</div>}
