import { ChevronLeft, ChevronRight, Loader2 } from "lucide-react";
export default function DatasetPreviewTable({preview,status,error,onPageChange}){if(status==="loading"&&!preview)return <div className="flex items-center gap-2 rounded-2xl border p-8 text-sm text-neutral-500"><Loader2 className="h-4 w-4 animate-spin"/>Loading real dataset rows…</div>;if(status==="error")return <div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>;if(!preview||!preview.columns.length)return <div className="rounded-2xl border p-8 text-sm text-neutral-500">No rows to preview.</div>;const from=preview.total_rows?((preview.page-1)*preview.page_size)+1:0;const to=Math.min(preview.page*preview.page_size,preview.total_rows);return <div className="overflow-hidden rounded-2xl border border-neutral-200 dark:border-neutral-800"><div className="overflow-x-auto"><table className="w-full min-w-max text-left text-sm"><thead><tr className="border-b bg-neutral-50 text-xs uppercase tracking-wider text-neutral-500 dark:border-neutral-800 dark:bg-neutral-900"><th className="sticky left-0 bg-neutral-50 px-4 py-3 dark:bg-neutral-900">#</th>{preview.columns.map(c=><th key={c} className="whitespace-nowrap px-4 py-3 font-medium">{c}</th>)}</tr></thead><tbody>{preview.rows.map((row,i)=><tr key={i} className="border-b last:border-0 hover:bg-neutral-50/70 dark:border-neutral-900 dark:hover:bg-neutral-900/60"><td className="sticky left-0 bg-white px-4 py-3 font-mono text-xs text-neutral-400 dark:bg-neutral-950">{from+i}</td>{preview.columns.map(c=><td key={c} className="max-w-[260px] whitespace-nowrap px-4 py-3">{row[c]??<span className="text-neutral-300">null</span>}</td>)}</tr>)}</tbody></table></div><div className="flex flex-wrap items-center justify-between gap-3 border-t px-4 py-3 text-xs text-neutral-500 dark:border-neutral-800"><span>Rows {from}–{to} of {preview.total_rows.toLocaleString()} · Page {preview.page} of {preview.total_pages}</span><div className="flex gap-2"><button disabled={preview.page<=1||status==="loading"} onClick={()=>onPageChange(preview.page-1)} className="flex items-center gap-1 rounded-lg border px-3 py-1.5 disabled:opacity-40"><ChevronLeft className="h-3.5 w-3.5"/>Previous</button><button disabled={preview.page>=preview.total_pages||status==="loading"} onClick={()=>onPageChange(preview.page+1)} className="flex items-center gap-1 rounded-lg border px-3 py-1.5 disabled:opacity-40">Next<ChevronRight className="h-3.5 w-3.5"/></button></div></div></div>}
